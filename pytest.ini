[tool:pytest]
testpaths = tests/task4
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: Unit tests
    integration: Integration tests
    slow: Slow running tests
addopts =
    --strict-markers
    --verbose
    --tb=short
    --color=yes
